const SERVER_NAME = 'localhost'
const SERVER_PORT = "3001"

export const URL_INSERT_PRODUCT = `http://${SERVER_NAME}:${SERVER_PORT}/products`
export const URL_UPDATE_PRODUCT = `http://${SERVER_NAME}:${SERVER_PORT}/products`
export const URL_DELETE_PRODUCT = `http://${SERVER_NAME}:${SERVER_PORT}/products`
export const URL_QUERY_PRODUCT = `http://${SERVER_NAME}:${SERVER_PORT}/products`
